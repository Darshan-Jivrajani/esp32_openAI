# esp32_openAI

# ESP32 OpenAI API Integration

## Setup & Usage

Run the following commands to update your system, install dependencies, and execute the application:

```bash
sudo apt-get update
sudo apt-get upgrade
pip install openai
export OPENAI_API_KEY="sk-xxxxxxxx"
python ai_server.py
